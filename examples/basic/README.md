# a11y-notify — Basic Example

This example demonstrates minimal `a11y-notify` usage.

## Prerequisites

Build the package first:

```bash
cd ../..
npm ci
npm run build
```

Then open `index.html` in a browser.
